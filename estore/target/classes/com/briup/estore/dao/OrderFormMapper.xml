<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
  "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.briup.estore.dao.BookMapper">
	<!-- 插入一个订单 -->
	<insert id="insertOrder" parameterType="OrderForm">
		insert into es_orderform)(id,cost,orderDate,shopAddress_id,customer_id)
		value(#{id},#{cost},#{orderdate},#{shopAddress.id},#{customer.id})
	</insert>
</mapper>